import AppKit

/// Grid of recent captures with copy / save / annotate / pin actions.
/// Stub — implemented by the capture-history work stream.
@MainActor
final class HistoryWindowController {
    static let shared = HistoryWindowController()

    func show() {
        NSLog("Capture history window not implemented yet")
    }
}
